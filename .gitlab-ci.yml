image: marcelhuberfoo/cabal-build


stages:
    - build


compile:
    stage: build
    script:
      - pacman -Sy --noconfirm ghc-static
      - cabal update
      - cabal install --only-dependencies
      - cabal build
  
